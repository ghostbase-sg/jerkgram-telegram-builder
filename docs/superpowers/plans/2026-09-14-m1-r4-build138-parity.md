# M1-R4 Build138 Settings Parity Implementation Plan

**Goal:** Replace the incomplete staged payload and rejected M1 implementation with a complete M1-R4 Settings-only product matching the reachable Jerkgram 1.0.2 Stable / Build138 Settings contract from source-export commit `1d2b7cf728fe63bc82f0dac8abdf27701d53f44d`.

**Authority:** Only the five final materialized Build138 owners named by the user plus their directly referenced native layout owners. No Build139+ or earlier test-build behavior may define UI, defaults, types, routes, or actions.

## Task 1: Establish a failing Build138-only verifier

- Replace the old string-presence tests that bless `dlsym`, raw ivar reads, navigation-bar insertion, and plain-controller pushes.
- Encode the exact eight-row main graph, reachable subpages, Build138 defaults/types, localization parity, forbidden `.root` route, and forbidden M2/runtime hooks.
- Add structural tests for Display-host-only pushes, semantic fail-closed account resolution, stable non-accumulating layout ownership, and complete Data & Backup/Stars/About/Debug actions.
- Run the verifier and retain the expected RED output against `ba490d8`.

## Task 2: Materialize the complete R4 source tree

- Recover the complete files present in payload parts 1/5–2/5, then remove `.m1r4-payload` from the materialized revision.
- Complete SettingsStore, localization, Settings UI, Telegram 12.9.4 adapter, bootstrap, schema, docs and workflow as ordinary reviewable source files.
- Keep feature logic separate from `Adapters/Telegram1294` and add no feature-runtime hook.

## Task 3: Match Build138 Settings behavior

- Implement the exact eight-row Settings section after My Profile and before Proxy, with exact titles, icon identities/RGB values, and only the Build138 reachable destinations.
- Keep Telegram as authoritative layout owner; compute every pass from current native geometry, store no translated baseline, and make removal/reapplication deterministic.
- Host every pushed page in a real runtime `Display.ViewController`.
- Reproduce Build138 home, Ghost Mode, Messages, Protected Content, Media & Stories, Appearance, Debug / Research, About, Stars, Send Style and Data & Backup behavior without exposing `.root`.

## Task 4: Match Build138 persistence and identity

- Implement semantic/fail-closed account resolution with no offsets, `memcpy`, `dlsym`, fabricated Swift values, or account 0.
- Derive settings definitions/defaults/types only from final Build138 source. Omit later-only Download Boost requirements from M1 parity.
- Enforce account isolation, cached access, canonical-wins/non-destructive legacy migration, separate telemetry preference, and separate retention model.

## Task 5: Reach GREEN and verify packaging invariants

- Run the focused verifier until GREEN, then run the complete repository test suite.
- Verify no notification/APNs/branding code changed, compatibility/carrier hashes remain frozen, and no M2 hook exists.
- Run static Mach-O/package checks defined by the workflow.

## Task 6: Build once and package deliverables

- Commit and push the materialized R4 revision to the requested R4 branch.
- Dispatch exactly one Apple/Xcode workflow run.
- Download and verify the single M1 device-test IPA.
- Create one checkpoint ZIP containing source revision metadata, verifier evidence, hashes, docs, and the IPA; publish both artifacts for handoff.
